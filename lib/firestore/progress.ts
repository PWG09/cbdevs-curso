import {collection,doc,getDoc,getDocs,query,where,serverTimestamp,setDoc} from "firebase/firestore"; import {db} from "@/lib/firebase/client"; import type {Progress} from "@/types/course";
export async function getLessonProgress(uid:string,lessonId:string){const s=await getDoc(doc(db,"progress",`${uid}_${lessonId}`));return s.exists()?({id:s.id,...s.data()} as Progress):null}
export async function setLessonCompleted(uid:string,courseId:string,phaseId:string,lessonId:string,completed:boolean){await setDoc(doc(db,"progress",`${uid}_${lessonId}`),{userId:uid,courseId,phaseId,lessonId,completed,completedAt:completed?serverTimestamp():null},{merge:true})}
export async function getCourseProgress(uid:string,courseId:string){const s=await getDocs(query(collection(db,"progress"),where("userId","==",uid),where("courseId","==",courseId)));return s.docs.map(d=>({id:d.id,...d.data()} as Progress))}
