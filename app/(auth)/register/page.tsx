"use client"; import {FormEvent,useState} from "react"; import Link from "next/link"; import {useRouter} from "next/navigation"; import {useAuth} from "@/providers/auth"; export default function Register(){const {register}=useAuth();const r=useRouter();const [n,setN]=useState("");const [e,setE]=useState("");const [p,setP]=useState("");const [err,setErr]=useState("");async function submit(x:FormEvent){x.preventDefault();setErr("");try{await register(n,e,p);r.replace("/my-courses")}catch(x){setErr(x instanceof Error?x.message:"No se pudo crear la cuenta")}}return <main className="shell"><div className="form card"><h1 className="title">Crear cuenta</h1><p className="muted">Las cuentas públicas se crean como cliente.</p><form className="stack" onSubmit={submit}><div><label className="label">Nombre</label><input className="input" value={n} onChange={x=>setN(x.target.value)} required/></div><div><label className="label">Correo</label><input className="input" type="email" value={e} onChange={x=>setE(x.target.value)} required/></div><div><label className="label">Contraseña</label><input className="input" type="password" minLength={6} value={p} onChange={x=>setP(x.target.value)} required/></div>{err&&<div className="error">{err}</div>}<button className="btn primary">Registrarme</button></form><p className="muted">¿Ya tienes cuenta? <Link href="/login">Entrar</Link></p></div></main>}
