"use client"; import {FormEvent,useState} from "react"; import Link from "next/link"; import {useAuth} from "@/providers/auth"; export default function Reset(){const {reset}=useAuth();const [e,setE]=useState("");const [msg,setMsg]=useState("");const [err,setErr]=useState("");async function submit(x:FormEvent){x.preventDefault();setMsg("");setErr("");try{await reset(e);setMsg("Revisa tu correo para restablecer la contraseña.")}catch(x){setErr(x instanceof Error?x.message:"No se pudo enviar el correo")}}return <main className="shell"><div className="form card"><h1 className="title">Restablecer contraseña</h1><form className="stack" onSubmit={submit}><input className="input" type="email" placeholder="Correo" value={e} onChange={x=>setE(x.target.value)} required/>{err&&<div className="error">{err}</div>}{msg&&<div className="success">{msg}</div>}<button className="btn primary">Enviar correo</button></form><p><Link href="/login">Volver</Link></p></div></main>}
