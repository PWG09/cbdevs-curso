"use client"; import {useEffect,useState} from "react"; import Guard from "@/components/Guard"; import {collection,getDocs,query,where} from "firebase/firestore"; import {db} from "@/lib/firebase/client"; import type {AppUser,Progress} from "@/types/course"; export default function Students(){return <Guard roles={["admin","empleado"]}><Inner/></Guard>}function Inner(){const [rows,setRows]=useState<AppUser[]>([]);useEffect(()=>{getDocs(query(collection(db,"users"),where("role","==","cliente"))).then(s=>setRows(s.docs.map(d=>({id:d.id,...d.data()} as AppUser))))},[]);return <section><h1 className="title">Alumnos</h1><div className="list">{rows.map(x=><div className="item" key={x.id}><div><b>{x.name}</b><div className="muted">{x.email}</div></div><span className="pill">{x.active?"Activo":"Inactivo"}</span></div>)}</div></section>}
