import "./globals.css"; import {AuthProvider} from "@/providers/auth"; export const metadata={title:"CBDEVS Cursos",description:"Plataforma de cursos CBDEVS"}; export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="es"><body><AuthProvider>{children}</AuthProvider></body></html>}
